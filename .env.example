# -----------------------------------------------------------------------------
#                           ОСНОВНЫЕ НАСТРОЙКИ БОТА
# -----------------------------------------------------------------------------
# ВАЖНО: Значения переменных указываются ПОСЛЕ знака '=' БЕЗ каких-либо кавычек (одинарных или двойных)
# и без лишних пробелов в начале или конце значения, если только пробелы не являются частью самого значения.

# ОБЯЗАТЕЛЬНО: Токен вашего Telegram-бота, полученный от @BotFather.
# Пример: 1234567890:AaBbCcDdEeFfGgHhIiJjKkLlMmNnOoPpQ
TELEGRAM_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN_HERE

# Режим запуска бота.
# Доступные значения: polling или webhook
# Если не указан, по умолчанию используется "polling".
BOT_RUN_MODE=polling


# -----------------------------------------------------------------------------
#                       НАСТРОЙКИ ДЛЯ РЕЖИМА WEBHOOK
#              (Эти параметры используются, только если BOT_RUN_MODE=webhook)
# -----------------------------------------------------------------------------

# ОБЯЗАТЕЛЬНО для webhook: Базовый публичный URL, на который Telegram будет отправлять обновления.
# Этот URL должен быть доступен из интернета и поддерживать HTTPS.
# Указывайте БЕЗ пути в конце (путь задается в WEBHOOK_PATH).
# Пример правильного значения: https://your.domain.com
# Пример правильного значения от ngrok: https://abcdef123.ngrok.io
WEBHOOK_URL=YOUR_PUBLIC_HTTPS_URL_HERE

# ОБЯЗАТЕЛЬНО для webhook: Порт, на котором ваше приложение (aiohttp) будет слушать входящие HTTP запросы.
# Этот порт будет использоваться внутри контейнера или на вашем сервере.
# Пример: 8080
# Пример: 8443
# Пример: 3000
# (В коде env_settings.py по умолчанию 8443, если не задан, но валидатор требует его для webhook).
WEBHOOK_PORT=8080

# ОБЯЗАТЕЛЬНО для webhook: Секретный токен для проверки подлинности запросов от Telegram.
# Telegram будет включать этот токен в заголовок X-Telegram-Bot-Api-Secret-Token.
# Сгенерируйте случайную безопасную строку, состоящую из букв (A-Z, a-z), цифр (0-9),
# символов подчеркивания (_) и дефиса (-). Длина от 1 до 256 символов.
# Указывайте БЕЗ КАВЫЧЕК.
# Пример правильного значения: MySuperSecretToken123_for-Bot
WEBHOOK_SECRET=YOUR_GENERATED_SECRET_TOKEN_HERE

# Путь на сервере, по которому aiohttp будет ожидать POST-запросы от Telegram.
# ОБЯЗАТЕЛЬНО должен начинаться с символа "/".
# Пример: /mybot/updates
# Пример: /webhook_supersecret_xyz
# ⚠️ ВАЖНО: Если используется реверс-прокси (например, nginx), то соответствующий путь
# должен быть проброшен через proxy_pass в конфигурации nginx!
WEBHOOK_PATH=/your_webhook_path_here


# -----------------------------------------------------------------------------
#          НАСТРОЙКИ SSL-СЕРТИФИКАТОВ ДЛЯ РЕЖИМА WEBHOOK (ОПЦИОНАЛЬНО)
#    (Используются, только если BOT_RUN_MODE=webhook И когда ваше приложение 
#   aiohttp не обрабатывает сырой HTTPS(этим занимается реверс-прокси, например nginx))
# -----------------------------------------------------------------------------
# Путь к публичному SSL-сертификату (в PEM-формате), который будет передан Telegram
# при установке webhook через метод setWebhook().
#
# Этот сертификат должен соответствовать тому, который используется на стороне
# reverse proxy (например, nginx), обслуживающего HTTPS. Это позволяет Telegram
# верифицировать защищённость вашего webhook URL.
#
# ВАЖНО: Этот путь используется ТОЛЬКО для передачи сертификата в Telegram.
# Пример: /etc/ssl/certs/nginx_public.pem
# Пример: /usr/src/app/fullchain.pem
# Если reverse proxy уже обслуживает HTTPS и Telegram может сам получить сертификат —
# эту переменную можно закомментировать.
WEBHOOK_CERT_PATH=/your/path/to/nginx_public_cert.pem